apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: observability-root-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/GreatOmotayo/azure-aks-observability-platform"
    targetRevision: main
    path: gitops/apps
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true